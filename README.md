# nodejs-test — Socket.IO demo

A tiny Node.js + Express + Socket.IO demo project.

Features
- Serves a static `public/index.html` that connects to the server using Socket.IO
- Simple server that emits a `hello` message to each client on connection

Quick start

1. Install dependencies

```powershell
npm install
```

2. Start the server

```powershell
npm start
```

3. Open http://localhost:3000 in your browser

Notes
- This repo is intended to be pushed to GitHub. See instructions below for creating a remote and pushing your local branch.

---

Prepared by GitHub Copilot (local workspace assistant).
